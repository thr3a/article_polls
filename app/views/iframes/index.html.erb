<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <%= javascript_include_tag 'iframe' %>
  <style>
  ul {
    padding: 0;
    position: relative;
  }

  ul li {
    border: solid 1px #2d8fdd;
    background: #f1f8ff;
    margin-bottom: 3px;
    line-height: 1.5;
    padding: 0.5em;
    list-style-type: none;
  }
  </style>
</head>
<body>
  <h3>この記事は参考になりましたか？</h3>
  <ul id="votes">
    <%= form_with url: votes_path, model: Vote.new do |f| %>
      <%= f.hidden_field :user_id, value: @uid %>
      <%= f.hidden_field :url, value: request.original_url %>
      <li>
        <%= button_tag '投票', value: '1', name: "vote[value]" %>
        <span class="vote_label">役に立った</span>
        <span class"vote_num"></span>
      </li>
      <li>
        <%= button_tag '投票', value: '-1', name: "vote[value]" %>
        <span class"vote_label">役に立たなかった</span>
      </li>
      <li>
        <%= button_tag '投票', value: '0', name: "vote[value]" %>
        <span class="vote_label">どちらでもない</span>
      </li>
    <% end %>
  </ul>
</body>
</html>
  